# Container Build Environment

Build the container image before running any container commands:

```
scripts/container-build-image.sh
```

Run commands inside the container:

```
scripts/container-run.sh just init
scripts/container-run.sh just build all
scripts/container-run.sh just draw
```
